<div class="btn-group btn-group-{{ size }}" role="group">
    {% for button in buttons %}
    <button type="button" class="btn btn-{{ button.type }}">{{ button.text }}</button>
    {% endfor %}
</div>