<div class="card">
    <div class="card-header">
        <h4 class="card-title">{{ morris.title }}</h4>
    </div>
    <div class="card-body">
        <div id="{{ morris.id }}" class="morris_chart_height"></div>
    </div>
</div>