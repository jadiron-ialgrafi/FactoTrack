<div class="media">
    <img class="align-self-start mr-3" src="{{ imageSrc }}" alt="{{ altText }}">
    <div class="media-body">
        <h5 class="mt-0">{{ heading }}</h5>
        <p>{{ description }}</p>
    </div>
</div>