<button type="{{ type }}" class="btn btn-dark {{ classes }}">
    {{ label }}
    {% if icon %}
        <span class="btn-icon-{{ iconPosition }}"><i class="{{ icon }}"></i></span>
    {% endif %}
</button>
