<div class="card">
    <div class="card-header">
        <h4 class="card-title">{{ chart.title }}</h4>
    </div>
    <div class="card-body">
        <canvas id="{{ chart.id }}"></canvas>
    </div>
</div>