 <ul class="list-group">
    {% for item in items %}
    <li class="list-group-item d-flex justify-content-between align-items-center">
        {{ item.text }}
        <span class="badge badge-primary badge-pill">{{ item.badge }}</span>
    </li>
    {% endfor %}
</ul>