<div class="list-group">
    {% for item in items %}
    <a href="{{ item.link }}" class="list-group-item list-group-item-action {% if item.active %}active{% endif %} {% if item.disabled %}disabled{% endif %}">
        {{ item.text }}
    </a>
    {% endfor %}
</div>
